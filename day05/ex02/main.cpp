#include "Bureaucrat.hpp"
#include "Form.hpp"
#include <iostream>

int main() {
    
    // Bureaucrat burec("Petya", 10);
    // Form UnForm("Dibilnaya", 11);
    // Form CoForm("Dibilnaya2", 9);
    // burec.signForm(UnForm);
    // burec.signForm(CoForm);

    return (0);
}